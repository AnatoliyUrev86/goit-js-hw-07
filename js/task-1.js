const listEl = document.querySelectorAll(".item-list");
console.log(listEl);

const itemEl = document.querySelectorAll(".item");
console.log(itemEl);

// const titleEl = document.querySelectorAll(".title");
// console.log(titleEl);

const titleEl = document.querySelectorAll(".title");
titleEl.textContent = "Animals";
console.log(titleEl);
